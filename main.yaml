- hosts: all
  become: true
  collections:
    - community.docker
  roles:
    - role: Oefenweb.fail2ban
      vars:
        - name: sshd
          port: "{{ ansible_port }}"
          maxretry: 3
          bantime: 86400 # 24h in sec
    - role: kvnmix
